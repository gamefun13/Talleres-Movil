<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Esta línea es para permitir acceso al directorio de caché interno -->
    <cache-path
        name="cache"
        path="." />

    <!-- Esta línea permite acceso al almacenamiento externo privado (si lo usas) -->
    <external-files-path
        name="external_files"
        path="." />

</paths>
